bool solve(int i, bool flag) {
    if (flag) {
        // Still matching target exactly
        // Need ch >= target[i] to stay valid
    } else {
        // Already greater than target
        // Can use any character freely (fillRem)
    }
}